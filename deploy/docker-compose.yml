name: grtblog
services:
  server:
    image: "${IMAGE_REPO_PREFIX:-}grtblog-server:${APP_VERSION:-local}"
    build:
      context: ..
      dockerfile: deploy/docker/server.Dockerfile
      args:
        APP_VERSION: "${APP_VERSION:-local}"
    container_name: grtblog-server
    environment:
      APP_ENV: production
      APP_PORT: "8080"
      APP_PROXY_HEADER: "X-Forwarded-For"
      APP_TRUSTED_PROXY_CHECK: "true"
      APP_IP_VALIDATION: "true"
      DB_DRIVER: postgres
      DB_DSN: "postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-change-me}@postgres:5432/${POSTGRES_DB:-grtblog}?sslmode=disable"
      REDIS_ADDR: redis:6379
      REDIS_PASSWORD: "${REDIS_PASSWORD:-}"
      REDIS_DB: "0"
      REDIS_PREFIX: "${REDIS_PREFIX:-grtblog:}"
      HTMLSNAPSHOT_BASE_URL: "http://renderer:3000"
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      renderer:
        condition: service_started
    volumes:
      - ./storage/html:/app/storage/html
      - ./storage/uploads:/app/storage/uploads
      - ./storage/geoip:/app/storage/geoip
    networks:
      - grtblog-internal
    restart: unless-stopped

  migrate:
    image: "${IMAGE_REPO_PREFIX:-}grtblog-server:${APP_VERSION:-local}"
    container_name: grtblog-migrate
    environment:
      DB_DSN: "postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-change-me}@postgres:5432/${POSTGRES_DB:-grtblog}?sslmode=disable"
    depends_on:
      postgres:
        condition: service_healthy
    command:
      [
        "goose",
        "-table",
        "public.goose_db_version",
        "-dir",
        "/app/migrations",
        "postgres",
        "postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-change-me}@postgres:5432/${POSTGRES_DB:-grtblog}?sslmode=disable",
        "up"
      ]
    networks:
      - grtblog-internal
    restart: "no"

  renderer:
    image: "${IMAGE_REPO_PREFIX:-}grtblog-renderer:${APP_VERSION:-local}"
    build:
      context: ..
      dockerfile: deploy/docker/web.Dockerfile
    container_name: grtblog-renderer
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "3000"
      INTERNAL_API_BASE_URL: "http://server:8080/api/v2"
    networks:
      - grtblog-internal
    restart: unless-stopped

  admin:
    image: "${IMAGE_REPO_PREFIX:-}grtblog-admin:${APP_VERSION:-local}"
    build:
      context: ..
      dockerfile: deploy/docker/admin.Dockerfile
    container_name: grtblog-admin
    networks:
      - grtblog-internal
    restart: unless-stopped

  postgres:
    image: postgres:17-alpine
    container_name: grtblog-postgres
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-grtblog}"
      POSTGRES_USER: "${POSTGRES_USER:-postgres}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-change-me}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-grtblog}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - grtblog-internal
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: grtblog-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    networks:
      - grtblog-internal
    restart: unless-stopped

volumes:
  postgres_data:
    name: grtblog_postgres_data
  redis_data:
    name: grtblog_redis_data

networks:
  grtblog-internal:
    name: grtblog-internal
    driver: bridge
